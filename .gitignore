# ============================================================
# DATA — never commit (Keepa data is commercial/licensed; not needed for serving)
# ============================================================
data/
raw/
*.parquet
*.csv
*.tsv
*.feather
con_KeepaExport_merged_raw.parquet
# Exception: small curated, processed (not raw) reference sample for Evidently
!monitoring/reference_sample.parquet

# ============================================================
# Terraform
# ============================================================
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
# tfvars holds sensitive values (email, IP); the example file is committed
*.tfvars
!*.tfvars.example
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
# .terraform.lock.hcl is intentionally committed (provider pinning) -> NOT ignored

# ============================================================
# Secrets / credentials
# ============================================================
.env
.env.*
*.pem
*.key
*_rsa
aws-credentials*
credentials
kubeconfig
*.kubeconfig

# ============================================================
# Python
# ============================================================
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
env/
.ipynb_checkpoints/

# ============================================================
# MLflow / local experiment outputs
# ============================================================
mlruns/
mlflow.db
*.sqlite

# ============================================================
# OS / editor
# ============================================================
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
